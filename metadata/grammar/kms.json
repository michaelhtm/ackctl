{
  "Name": "kms",
  "Resources": [
    {
      "Name": "alias",
      "ARNFormats": [
        "arn:${Partition}:kms:${Region}:${Account}:alias/${Alias}"
      ]
    },
    {
      "Name": "key",
      "ARNFormats": [
        "arn:${Partition}:kms:${Region}:${Account}:key/${KeyId}"
      ]
    }
  ]
}
